# Vector Search System Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                          UFDR Forensic Agent                         │
│                    (realtime/utils/ai/agent.py)                      │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ Has access to
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
        ▼                           ▼                           ▼
┌──────────────┐          ┌──────────────────┐         ┌──────────────┐
│ Traditional  │          │  Vector Search   │         │    Other     │
│    Tools     │          │      Tool        │         │    Tools     │
├──────────────┤          ├──────────────────┤         ├──────────────┤
│ • Messages   │          │ • Semantic       │         │ • Locations  │
│ • Contacts   │          │   Search         │         │ • Apps       │
│ • Call Logs  │          │ • Multi-Modal    │         │ • Browsing   │
│ • Exact      │          │ • Embeddings     │         │              │
│   Filters    │          │ • Similarity     │         │              │
└──────────────┘          └──────────────────┘         └──────────────┘
        │                           │                           │
        │                           │                           │
        ▼                           ▼                           ▼
┌──────────────┐          ┌──────────────────┐         ┌──────────────┐
│  PostgreSQL  │          │     Qdrant       │         │  PostgreSQL  │
│   Database   │          │  Vector Store    │         │   Database   │
└──────────────┘          └──────────────────┘         └──────────────┘
```

## Vector Search Flow

```
User Query: "Tell me about crypto trading messages"
    │
    ▼
┌───────────────────────────────────────────────────────────────┐
│  1. FORENSIC AGENT                                             │
│     Receives query and decides which tool to use               │
│     Decision: Use vector_search (semantic search needed)       │
└───────────────────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────────────────┐
│  2. VECTOR SEARCH TOOL                                         │
│     realtime/tools/vector_search.py                            │
│                                                                 │
│     Input:                                                      │
│     • query: "Tell me about crypto trading messages"           │
│     • collection: "messages"                                   │
│     • limit: 5                                                 │
│     • score_threshold: 0.7                                     │
└───────────────────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────────────────┐
│  3. GENERATE EMBEDDING                                         │
│     realtime/utils/vector/embeddings.py                        │
│                                                                 │
│     SentenceTransformer Model:                                 │
│     • Model: all-MiniLM-L6-v2                                  │
│     • Input: "Tell me about crypto trading messages"           │
│     • Output: [0.234, -0.123, ..., 0.789] (384 dimensions)     │
└───────────────────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────────────────┐
│  4. SEARCH QDRANT                                              │
│     Vector similarity search using HNSW index                  │
│                                                                 │
│     Query Vector: [0.234, -0.123, ..., 0.789]                  │
│     Collection: "messages"                                     │
│     Distance Metric: Cosine Similarity                         │
│     Limit: 5 results                                           │
│     Threshold: 0.7                                             │
└───────────────────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────────────────┐
│  5. RETRIEVE RESULTS                                           │
│                                                                 │
│     Result 1: Score: 0.92                                      │
│     • "Let's discuss crypto trading strategies"                │
│     • From: Alice, App: WhatsApp                               │
│                                                                 │
│     Result 2: Score: 0.87                                      │
│     • "Bitcoin price is rising today"                          │
│     • From: Bob, App: Telegram                                 │
│                                                                 │
│     Result 3: Score: 0.84                                      │
│     • "Cryptocurrency investment opportunities"                │
│     • From: Charlie, App: WhatsApp                             │
│                                                                 │
│     ... (up to 5 results)                                      │
└───────────────────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────────────────┐
│  6. FORMAT RESULTS                                             │
│     Convert to human-readable summary                          │
│                                                                 │
│     🔍 Vector Search Results                                   │
│     ================                                           │
│     Query: 'Tell me about crypto trading messages'             │
│     Collection: messages                                       │
│     Results found: 3                                           │
│     ... (formatted output)                                     │
└───────────────────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────────────────┐
│  7. RETURN TO AGENT                                            │
│     Agent receives formatted results and responds to user      │
└───────────────────────────────────────────────────────────────┘
```

## Data Flow: Populating Vector Database

```
┌──────────────────┐
│   PostgreSQL     │
│   Messages DB    │
│                  │
│  ┌────────────┐  │
│  │ Messages   │  │
│  │ Table      │  │
│  └────────────┘  │
└──────────────────┘
        │
        │ SELECT body, metadata
        │ FROM messages
        ▼
┌──────────────────┐
│  Python Script   │
│  (Batch Proc.)   │
└──────────────────┘
        │
        │ 1. Fetch 1000 messages
        ▼
┌──────────────────────────────────┐
│  Generate Embeddings              │
│  realtime/utils/vector/           │
│                                   │
│  Input: ["msg1", "msg2", ...]     │
│  Model: SentenceTransformer       │
│  Output: [emb1, emb2, ...]        │
└──────────────────────────────────┘
        │
        │ 2. Batch embeddings
        ▼
┌──────────────────────────────────┐
│  Prepare Vectors                  │
│                                   │
│  vectors = [                      │
│    (id1, emb1, payload1),         │
│    (id2, emb2, payload2),         │
│    ...                            │
│  ]                                │
└──────────────────────────────────┘
        │
        │ 3. Insert batch
        ▼
┌──────────────────┐
│     Qdrant       │
│  Vector Store    │
│                  │
│  ┌────────────┐  │
│  │ messages   │  │
│  │ collection │  │
│  │            │  │
│  │ • Vectors  │  │
│  │ • Payloads │  │
│  │ • Index    │  │
│  └────────────┘  │
└──────────────────┘
```

## Component Architecture

```
realtime/
├── tools/
│   ├── vector_search.py          ◄── Agent-facing tool
│   ├── messages.py                   (Traditional filter)
│   ├── contacts.py                   (Traditional filter)
│   └── call_logs.py                  (Traditional filter)
│
├── utils/
│   ├── vector/
│   │   ├── __init__.py
│   │   └── embeddings.py         ◄── Core utilities
│   │       ├── get_qdrant_client()
│   │       ├── generate_text_embedding()
│   │       ├── generate_batch_embeddings()
│   │       ├── create_collection()
│   │       ├── insert_vector()
│   │       ├── insert_batch_vectors()
│   │       └── search_vectors()
│   │
│   ├── ai/
│   │   └── agent.py              ◄── Agent with all tools
│   │
│   └── db/
│       └── connection.py             (PostgreSQL)
│
examples/
└── vector_search_example.py      ◄── Working examples

scripts/
└── setup_qdrant.sh               ◄── Setup automation
```

## Technology Stack

```
┌─────────────────────────────────────────────────┐
│                 Application Layer                │
├─────────────────────────────────────────────────┤
│ • FastAPI (REST API)                             │
│ • Python 3.13                                    │
│ • Async/Await                                    │
└─────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│                   Agent Layer                    │
├─────────────────────────────────────────────────┤
│ • OpenAI Agents                                  │
│ • LiteLLM (Model abstraction)                    │
│ • Gemini 1.5 Flash                               │
└─────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│                   Tools Layer                    │
├─────────────────────────────────────────────────┤
│ • Traditional: PostgreSQL queries                │
│ • Vector Search: Qdrant semantic search          │
│ • Pydantic validation                            │
└─────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│                Embedding Layer                   │
├─────────────────────────────────────────────────┤
│ • SentenceTransformers                           │
│ • Model: all-MiniLM-L6-v2 (384-dim)              │
│ • Batch processing support                       │
└─────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│                 Storage Layer                    │
├─────────────────────────────────────────────────┤
│ • PostgreSQL (Structured data)                   │
│ • Qdrant (Vector embeddings)                     │
│ • HNSW indexing                                  │
└─────────────────────────────────────────────────┘
```

## Search Comparison

### Traditional Query (SQL-based)
```
User: "Get WhatsApp messages from Alice"
    │
    ▼
┌─────────────────────────────────┐
│  query_messages tool             │
│                                  │
│  Filters:                        │
│  • source_app: WhatsApp          │
│  • from_party_name: Alice        │
└─────────────────────────────────┘
    │
    ▼
PostgreSQL Query:
SELECT * FROM messages
WHERE source_app = 'WhatsApp'
  AND from_party_name = 'Alice'
    │
    ▼
Results: Exact matches only
```

### Vector Search (Semantic)
```
User: "Tell me about crypto trading"
    │
    ▼
┌─────────────────────────────────┐
│  vector_search tool              │
│                                  │
│  Converts to embedding:          │
│  [0.234, -0.123, ..., 0.789]     │
└─────────────────────────────────┘
    │
    ▼
Qdrant Vector Search:
Find similar embeddings using
cosine similarity
    │
    ▼
Results: Semantic matches
• "crypto trading"      (0.95)
• "Bitcoin investment"  (0.87)
• "cryptocurrency"      (0.84)
• "digital currency"    (0.78)
```

## Scaling Considerations

### Small Scale (< 100K vectors)
```
┌─────────────────┐
│  Single Qdrant  │
│    Container    │
│                 │
│  Memory: 2GB    │
│  CPU: 2 cores   │
└─────────────────┘
```

### Medium Scale (100K - 1M vectors)
```
┌─────────────────┐
│  Single Qdrant  │
│    Instance     │
│                 │
│  Memory: 8GB    │
│  CPU: 4 cores   │
│  SSD Storage    │
└─────────────────┘
```

### Large Scale (> 1M vectors)
```
┌─────────────────┐     ┌─────────────────┐
│  Qdrant Node 1  │     │  Qdrant Node 2  │
│  (Shard 1)      │     │  (Shard 2)      │
└─────────────────┘     └─────────────────┘
        │                       │
        └───────────┬───────────┘
                    │
            ┌───────────────┐
            │  Load Balancer │
            └───────────────┘
```

## Performance Metrics

### Embedding Generation
```
Single text:     10-50ms
Batch (100):     100-500ms
Batch (1000):    1-5 seconds

Recommendation: Use batch processing
```

### Vector Search
```
1K vectors:      1-2ms
100K vectors:    2-5ms
1M vectors:      5-10ms
10M vectors:     10-20ms

Index: HNSW (Hierarchical Navigable Small World)
Precision: ~99% (vs exact search)
```

### Storage
```
Vector (384-dim):     ~1.5 KB
Payload (typical):    ~0.5 KB
Total per message:    ~2 KB

1M messages:          ~2 GB
10M messages:         ~20 GB
```

## Security Considerations

```
┌─────────────────────────────────────────────────┐
│                Security Layers                   │
├─────────────────────────────────────────────────┤
│                                                  │
│  1. API Authentication                           │
│     • JWT tokens                                 │
│     • API keys                                   │
│                                                  │
│  2. Network Security                             │
│     • Qdrant: TLS/SSL                            │
│     • PostgreSQL: SSL connections                │
│                                                  │
│  3. Data Privacy                                 │
│     • Embeddings don't expose raw data           │
│     • Payloads encrypted at rest                 │
│                                                  │
│  4. Access Control                               │
│     • Collection-level permissions               │
│     • Query filtering                            │
│                                                  │
└─────────────────────────────────────────────────┘
```

## Deployment Architecture

```
┌────────────────────────────────────────────────────────┐
│                    Production Setup                     │
└────────────────────────────────────────────────────────┘

┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   FastAPI    │────▶│   Agent      │────▶│   Tools      │
│   Server     │     │   Service    │     │   Layer      │
└──────────────┘     └──────────────┘     └──────────────┘
       │                                          │
       │                                          │
       ▼                                          ▼
┌──────────────┐                          ┌──────────────┐
│  PostgreSQL  │                          │   Qdrant     │
│   Database   │                          │   Cluster    │
│              │                          │              │
│  • Messages  │                          │  • Vectors   │
│  • Contacts  │                          │  • HNSW      │
│  • Call Logs │                          │  • Sharding  │
└──────────────┘                          └──────────────┘

All components run in Docker containers
Orchestrated with Docker Compose or Kubernetes
```

## Summary

This architecture provides:
- **Separation of Concerns**: Clear boundaries between components
- **Scalability**: Horizontal scaling for vector database
- **Performance**: Fast semantic search with HNSW indexing
- **Flexibility**: Multiple search modalities (exact + semantic)
- **Maintainability**: Well-organized, documented codebase
